<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mailcursor.mapper.SysRoleMapper">

    <resultMap id="SysRoleMap" type="com.mailcursor.system.model.SysRole">
        <id property="id" column="id"/>
        <result property="roleCode" column="role_code"/>
        <result property="roleName" column="role_name"/>
        <result property="status" column="status"/>
        <result property="remark" column="remark"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="selectAll" resultMap="SysRoleMap">
        SELECT id, role_code, role_name, status, remark, created_at, updated_at
        FROM sys_role ORDER BY id ASC
    </select>

    <select id="selectById" resultMap="SysRoleMap">
        SELECT id, role_code, role_name, status, remark, created_at, updated_at
        FROM sys_role WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_role (role_code, role_name, status, remark, created_at, updated_at)
        VALUES (#{roleCode}, #{roleName}, #{status}, #{remark}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="update">
        UPDATE sys_role
        SET role_code = #{roleCode}, role_name = #{roleName}, status = #{status},
            remark = #{remark}, updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <select id="countUsersByRoleId" resultType="int">
        SELECT COUNT(1) FROM sys_user_role WHERE role_id = #{roleId}
    </select>

    <delete id="deleteRoleMenusByRoleId">
        DELETE FROM sys_role_menu WHERE role_id = #{roleId}
    </delete>

    <delete id="deleteById">
        DELETE FROM sys_role WHERE id = #{id}
    </delete>
</mapper>
